<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">

    <% if Helpers.is_logged_in?(session) %>
    <p>
      <label><%= Helpers.current_user(session).username %>: </label>
      <span><%= Helpers.current_user(session).balance %></span>
    </p>

    <p>
      <a href="/logout">Log Out</a>
    </p>
    <% end %>
  </body>
</html>
